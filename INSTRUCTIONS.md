# Basic Instructions
For all node package commands, use PNPM only, not npm.
For github, I've installed the github cli, `gh`. It is already logged in.
For atlassian products (JIRA / Confluence) you can use either the atlassian `acli` tool or the mcp server
